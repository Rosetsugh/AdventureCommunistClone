// Only used for the Layers within the Animation Controller
public static class LayerIndex
{
	// Apparently Layers on the Animation Controller do not require a bit shift
	public static int BaseLayer = 0;
}